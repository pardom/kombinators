package kombinators

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// O combinator - owl

fun <A, B> O(): (((A) -> B) -> A) -> ((A) -> B) -> B =
    { x -> { y -> y(x(y)) } }

fun <A, B> owl() = O<A, B>()
